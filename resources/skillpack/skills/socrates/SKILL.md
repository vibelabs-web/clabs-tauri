---
name: socrates
description: 소크라테스식 1:1 기획 컨설팅. 핵심 기능 도출 → 화면 매핑 → 7개 기획 문서 생성. /screen-spec과 연결.
---

# Socrates: 소크라테스식 기획 컨설팅

> "기능에 집중하고, 화면에 매핑하라"

---

## ⛔ 절대 금지

1. ❌ 질문 없이 기획서 작성 금지
2. ❌ 고정 질문 기계적 사용 금지
3. ❌ 반드시 AskUserQuestion 도구 사용

---

## 스킬 시작 시 필수 행동

```
0. MCP 사전 설정 체크 (중요!):
   - Stitch MCP 설정 상태 확인 및 사용자에게 표시
   - 아래 "MCP 사전 설정 체크" 섹션 참조

1. Read 도구로 references 파일 읽기:
   - references/level-assessment.md
   - references/socratic-method.md
   - references/dynamic-questions.md
   - references/conversation-rules.md
   - references/phase-details.md        ← Phase별 상세 가이드
   - references/feature-screen-mapping.md
   - references/tech-stack-recommendation.md

2. 사용자 프로필 확인:
   - .claude/memory/user-profile.md 읽기
   - 기존 사용자면 레벨 측정 건너뛰기

3. neurion-proposal.md 확인 (브레인스토밍 기획안):
   - 프로젝트 루트의 neurion-proposal.md 읽기
   - 존재하면 → Phase 0에서 기획안 활용 여부 질문
   - 없으면 → 기존 방식대로 진행
```

---

## MCP 사전 설정 체크 (스킬 시작 전 자동 실행)

> ⚠️ **/socrates 시작 시 반드시 MCP 설정 상태를 체크하고 사용자에게 표시합니다.**
> 이를 통해 /screen-spec Phase 5의 Stitch 디자인 생성 가능 여부를 미리 알 수 있습니다.

### 체크 시퀀스

```bash
# settings.json에서 MCP 설정 확인
cat ~/.claude/settings.json 2>/dev/null | grep -q '"stitch"' && STITCH="✅" || STITCH="❌"
cat ~/.claude/settings.json 2>/dev/null | grep -q '"gemini"' && GEMINI="✅" || GEMINI="❌"
cat ~/.claude/settings.json 2>/dev/null | grep -q '"context7"' && CONTEXT7="✅" || CONTEXT7="❌"
```

### 사용자에게 표시할 메시지

```
┌─────────────────────────────────────────────────────────┐
│  🔧 MCP 설정 상태                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  🎨 Stitch MCP: {✅ 설정됨 / ❌ 미설정}                   │
│     → /screen-spec Phase 5에서 디자인 목업 자동 생성     │
│                                                         │
│  🤖 Gemini MCP: {✅ 설정됨 / ❌ 미설정}                   │
│     → 프론트엔드 디자인 코딩 지원                        │
│                                                         │
│  📚 Context7 MCP: {✅ 설정됨 / ❌ 미설정}                 │
│     → 최신 라이브러리 문서 검색                          │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  💡 미설정 항목은 ./install.sh 재실행으로 설정 가능       │
└─────────────────────────────────────────────────────────┘
```

### ❌ Stitch MCP 미설정 시 안내 (API Key로 간단 설정!)

```
┌─────────────────────────────────────────────────────────┐
│  🎨 Stitch MCP 설정 (API Key로 간단!)                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  GCP 프로젝트 없이 API Key로 간단히 설정!               │
│                                                         │
│  📋 Step 1: API Key 생성                                │
│     👉 https://stitch.withgoogle.com/settings          │
│     → "Create Key" 클릭 → Key 복사                      │
│                                                         │
│  📋 Step 2: Claude Labs 인스톨러                        │
│     $ ./install.sh → Stitch MCP 선택 → API Key 입력    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### ❌ Gemini MCP 미설정 시 안내 (gcloud 필요)

```
┌─────────────────────────────────────────────────────────┐
│  🤖 Gemini MCP 설정 가이드 (ADC 인증 필요)              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📋 Step 1: gcloud CLI 설치                             │
│     https://cloud.google.com/sdk/docs/install          │
│                                                         │
│  📋 Step 2: ADC 인증                                    │
│     $ gcloud auth application-default login             │
│     → 브라우저에서 Google 로그인                        │
│                                                         │
│  📋 Step 3: Claude Labs 인스톨러                        │
│     $ ./install.sh → Gemini MCP 선택                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 CLI 하단 진행 상황 표시 (필수!)

> **모든 Phase 진입 시 TaskCreate를 사용하여 CLI 하단에 진행 상황을 표시합니다.**

### Phase별 TaskCreate 호출

```typescript
// Phase 0: MCP 설정 체크
TaskCreate({
  subject: "/socrates Phase 0: MCP 설정 체크",
  description: "Stitch, Gemini, Context7 MCP 설정 상태 확인",
  activeForm: "🔧 MCP 설정 상태 확인 중..."
})

// Phase 0.5: 레벨 측정
TaskCreate({
  subject: "/socrates Phase 0.5: 레벨 측정",
  description: "사용자 기술 수준 파악",
  activeForm: "📊 레벨 측정 중..."
})

// Phase 1: 핵심 기능 도출
TaskCreate({
  subject: "/socrates Phase 1: 핵심 기능 도출",
  description: "핵심 기능 3가지 도출",
  activeForm: "💬 핵심 기능 도출 중..."
})

// Phase 2: 화면 매핑
TaskCreate({
  subject: "/socrates Phase 2: 화면 매핑",
  description: "기능을 화면에 매핑",
  activeForm: "🗺️ 화면 매핑 진행 중..."
})

// Phase 2.5: 기술 스택 결정
TaskCreate({
  subject: "/socrates Phase 2.5: 기술 스택",
  description: "기술 스택 추천 및 결정",
  activeForm: "⚙️ 기술 스택 결정 중..."
})

// Phase 3: 문서 생성
TaskCreate({
  subject: "/socrates Phase 3: 문서 생성",
  description: "7개 기획 문서 생성",
  activeForm: "📄 기획 문서 생성 중... ({완료}/{7})"
})

// Phase 3.5: 기획 검증
TaskCreate({
  subject: "/socrates Phase 3.5: 기획 검증",
  description: "망하는 기획 4요건 검사",
  activeForm: "🔍 기획 검증 중..."
})
```

### Phase 완료 시

```typescript
TaskUpdate({
  taskId: "{현재 task id}",
  status: "completed"
})
```

---

## 워크플로우 요약

```
Phase 0: 초기 설정
├── Step 0: 사용자 프로필 확인 (자동)
├── Step 0.5: neurion-proposal.md 확인 (브레인스토밍 기획안)
├── Step 1: 슬랙 웹훅 설정 (선택)
└── Step 2: 레벨 측정 (신규 사용자만)
    ↓
Phase 1: 핵심 기능 도출
├── "무엇을 만들고 싶어요?"
├── "핵심 기능 3가지가 뭐예요?"
└── 기능별 Why 질문 (1~2회)
    ↓
Phase 2: 기능 → 화면 매핑 (핵심!)
├── 각 기능의 화면 상상
├── 화면 간 이동 경로
└── 컴포넌트 도출
    ↓
Phase 2.5: 기술 스택 결정
└── 레벨별 제안 전략 적용
    ↓
Phase 3: 7개 문서 생성
├── 01-prd.md ~ 07-coding-convention.md
└── 06-screens.md ← /screen-spec 입력
    ↓
Phase 3.5: 망하는 기획 4요건 검증
├── 벤치마킹, 가설/사실, 타협, 실험 검사
    ↓
⏭️ 다음 단계 선택
```

---

## Phase별 핵심 규칙

### Phase 0: 레벨 측정

| 레벨 | 질문 스타일 | 질문 수 |
|------|------------|---------|
| L1 (초심자) | 비유, 감정 중심 | 15~20개 |
| L2 (일반인) | 비유 + 기술 개념 | 12~18개 |
| L3 (경험자) | 구체적 기술 질문 | 10~15개 |
| L4 (전문가) | 아키텍처, 트레이드오프 | 10~12개 |

> 상세: `references/level-assessment.md`

### Phase 1: 핵심 기능 도출

**3가지 핵심 질문:**
1. "이 서비스에서 사용자가 가장 먼저 하는 행동이 뭔가요?"
2. "그 다음엔 뭘 하나요?"
3. "이 서비스 없이는 절대 안 되는 기능이 뭔가요?"

> 상세: `references/phase-details.md`

### Phase 2: 화면 매핑 (가장 중요!)

**완료 기준:**
- ✅ 모든 기능이 화면에 매핑됨
- ✅ 화면 간 이동 경로가 명확함
- ✅ 각 화면의 컴포넌트가 식별됨

> 상세: `references/feature-screen-mapping.md`

### Phase 2.5: 기술 스택

| 레벨 | 전략 |
|------|------|
| L1/L2 | 자동 추천 ("이대로 진행할게요") |
| L3/L4 | 선택지 제공 + 트레이드오프 설명 |

> 상세: `references/tech-stack-recommendation.md`

### Phase 3: 문서 생성

```
./docs/planning/
├── 01-prd.md
├── 02-trd.md
├── 03-user-flow.md
├── 04-database-design.md
├── 05-design-system.md
├── 06-screens.md          ← /screen-spec 입력
└── 07-coding-convention.md
```

### Phase 3.5: 기획 검증

4가지 함정 검사:
1. 벤치마킹 - 유사 서비스 분석
2. 가설/사실 - 검증 여부 구분
3. 타협 - 핵심 가치 유지
4. 실험 - MVP 및 실패 대응

> 상세: `references/failing-planning-checklist.md`

---

## ⏭️ 다음 단계 (CRITICAL)

> **이 섹션은 스킬 완료 후 반드시 실행합니다.**
> 참조: `~/.claude/docs/workflow-chain.md`

**기획 문서 생성 완료 후 AskUserQuestion 실행:**

```json
{
  "questions": [{
    "question": "소크라테스 대화가 완료되었습니다!\n\n📋 핵심 기능 도출 완료\n📱 화면 매핑 완료\n📄 7개 기획 문서 생성 완료\n\n다음 단계를 선택해주세요:",
    "header": "다음 단계",
    "options": [
      {"label": "/screen-spec 실행", "description": "화면별 상세 명세(YAML) 생성 - 권장"},
      {"label": "/tasks-generator 실행", "description": "화면 명세 없이 바로 TASKS.md 생성"}
    ],
    "multiSelect": false
  }]
}
```

**권장 워크플로우:**
```
/socrates → /screen-spec → /tasks-generator → /project-bootstrap → /auto-orchestrate
```

---

## Reference 파일

| 파일 | 내용 |
|------|------|
| `phase-details.md` | Phase별 상세 가이드 (질문 예시 포함) |
| `level-assessment.md` | 레벨 측정 시스템 |
| `socratic-method.md` | 소크라테스 질문 기법 |
| `dynamic-questions.md` | 동적 질문 생성 규칙 |
| `conversation-rules.md` | 대화 규칙 |
| `feature-screen-mapping.md` | 기능→화면 매핑 |
| `tech-stack-recommendation.md` | 기술 스택 제안 |
| `failing-planning-checklist.md` | 망하는 기획 4요건 |
| 템플릿 파일들 | prd, trd, screens 등 |
