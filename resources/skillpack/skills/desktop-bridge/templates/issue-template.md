# GitHub Issue 템플릿

> /desktop-bridge publish 시 사용되는 마스터 Issue 템플릿

---

## 템플릿 변수

| 변수 | 설명 | 예시 |
|------|------|------|
| `{project_name}` | 프로젝트 이름 | ShopEase |
| `{description}` | 프로젝트 한 줄 설명 | 소규모 상점을 위한 이커머스 플랫폼 |
| `{features}` | 핵심 기능 목록 | 상품 관리, 장바구니, 결제 |
| `{screens}` | 화면 체크리스트 | product-list, cart, checkout |
| `{frontend}` | 프론트엔드 기술 | React + Vite + TailwindCSS |
| `{backend}` | 백엔드 기술 | FastAPI + SQLAlchemy |
| `{database}` | 데이터베이스 | PostgreSQL |
| `{prd_summary}` | PRD 요약 | 제품 요구사항 핵심 내용 |
| `{trd_summary}` | TRD 요약 | 기술 요구사항 핵심 내용 |
| `{screen_yamls}` | 화면 명세 YAML | 각 화면의 YAML 내용 |
| `{timestamp}` | 생성 시각 | 2026-01-31T10:00:00Z |
| `{issue_number}` | Issue 번호 (생성 후) | 123 |

---

## 기본 템플릿

```markdown
## 📋 설계 요약

{description}

---

## 🎯 핵심 기능

{features_list}

---

## 📱 화면 목록

> 체크박스는 구현 진행에 따라 자동으로 업데이트됩니다.

{screens_checklist}

---

## 🔧 기술 스택

| 영역 | 기술 |
|------|------|
| **Frontend** | {frontend} |
| **Backend** | {backend} |
| **Database** | {database} |

---

## 📄 첨부 문서

<details>
<summary>📋 PRD (제품 요구사항)</summary>

{prd_summary}

</details>

<details>
<summary>🔧 TRD (기술 요구사항)</summary>

{trd_summary}

</details>

<details>
<summary>📱 화면 명세 상세</summary>

{screen_yamls}

</details>

---

## 🔗 CLI 연동

CLI에서 이 설계를 기반으로 구현을 시작하려면:

```bash
/desktop-bridge implement #{issue_number}
```

---

## 📊 진행 상황

| Phase | 상태 | 완료일 |
|-------|------|--------|
| Phase 0: 셋업 | ⏳ 대기 | - |
| Phase 1: 데이터베이스 | ⏳ 대기 | - |
| Phase 2: 백엔드 | ⏳ 대기 | - |
| Phase 3: 프론트엔드 | ⏳ 대기 | - |
| Phase 4: 통합 | ⏳ 대기 | - |

---

🤖 Generated by Claude Desktop via `/desktop-bridge publish`
📅 {timestamp}
```

---

## 섹션별 포맷

### 핵심 기능 (features_list)

```markdown
1. **{feature_name_1}**: {feature_description_1}
2. **{feature_name_2}**: {feature_description_2}
3. **{feature_name_3}**: {feature_description_3}
```

### 화면 체크리스트 (screens_checklist)

```markdown
- [ ] `{screen_id}`: **{screen_name}** - `{route}`
  - 주요 컴포넌트: {components}
  - 데이터: {data_requirements}
```

**예시**:
```markdown
- [ ] `product-list`: **상품 목록** - `/products`
  - 주요 컴포넌트: ProductGrid, FilterSidebar, Pagination
  - 데이터: products (id, name, price, thumbnail)

- [ ] `product-detail`: **상품 상세** - `/products/:id`
  - 주요 컴포넌트: ProductInfo, ImageGallery, AddToCartButton
  - 데이터: product (전체 필드), reviews

- [ ] `cart`: **장바구니** - `/cart`
  - 주요 컴포넌트: CartItemList, OrderSummary, CheckoutButton
  - 데이터: cartItems, totalPrice
```

### 화면 명세 YAML (screen_yamls)

```markdown
### {screen_name} ({screen_id})

\`\`\`yaml
version: "2.0"
screen:
  name: {screen_name}
  route: {route}
  layout: {layout}
  auth: {auth_required}

data_requirements:
  - resource: {resource}
    needs: [{fields}]

components:
  - id: {component_id}
    type: {component_type}
    description: {description}

tests:
  - name: {test_name}
    when: {condition}
    then: [{expected}]
\`\`\`

---

```

---

## 라벨

| 라벨 | 색상 | 설명 |
|------|------|------|
| `design` | #1D76DB | 설계/기획 Issue |
| `from-desktop` | #C2E0C6 | Claude Desktop에서 생성됨 |
| `v{version}` | #FBCA04 | 버전 태그 |
| `priority:high` | #D93F0B | 높은 우선순위 |
| `priority:medium` | #FBCA04 | 중간 우선순위 |
| `priority:low` | #0E8A16 | 낮은 우선순위 |

---

## 제목 형식

```
[Design] {project_name} - 아키텍처 및 화면 명세
```

**예시**:
```
[Design] ShopEase - 아키텍처 및 화면 명세
[Design] TaskFlow - 아키텍처 및 화면 명세
[Design] HealthTracker - 아키텍처 및 화면 명세
```
